name: Discover Unofficial Papers

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/discover-unofficial.yml'
      - 'scripts/discover-unofficial-papers.mjs'
      - 'scripts/lib/unofficial-papers.mjs'
      - 'scripts/lib/zhipu-client.mjs'
  schedule:
    - cron: '15 1 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: unofficial-discovery
  cancel-in-progress: true

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check Zhipu secret
        env:
          ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
        run: |
          if [ -z "$ZHIPU_API_KEY" ]; then
            echo "ZHIPU_API_KEY is not configured."
            exit 1
          fi

      - name: Refresh unofficial discoveries and rebuild assets
        timeout-minutes: 35
        env:
          ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
          ZHIPU_MODEL: glm-4.5-flash
          ZHIPU_SEARCH_TOOL: search_pro
          ZHIPU_READER_TOOL: reader
          ZHIPU_SEARCH_TIMEOUT_MS: 20000
          ZHIPU_READER_TIMEOUT_MS: 20000
          ZHIPU_CHAT_TIMEOUT_MS: 90000
          ZHIPU_RETRIES: 2
          ZHIPU_CHAT_RETRIES: 1
          ZHIPU_RATE_LIMIT_DELAY_MS: 120000
          DISCOVERY_BUDGET_MS: 1500000
          DISCOVERY_MAX_QUERIES: 18
          DISCOVERY_PLAN_STRATEGY: balanced
          DISCOVERY_MAX_RESULTS: 6
          DISCOVERY_MODEL_PLANNING: false
          DISCOVERY_MAX_MODEL_QUERIES: 0
          DISCOVERY_MAX_FOLLOW_UP_QUERIES: 4
          DISCOVERY_MAX_READERS: 14
          DISCOVERY_MIN_READERS: 8
          DISCOVERY_MAX_FALLBACK_CANDIDATES: 12
          DISCOVERY_BATCH_SIZE: 1
          DISCOVERY_MAX_BATCHES: 4
          DISCOVERY_REFINE_BATCH_SIZE: 1
          DISCOVERY_ABORT_REFINEMENT_ON_RATE_LIMIT: true
          DISCOVERY_EXTRACTION_DELAY_MS: 120000
        run: npm run pipeline:refresh:discover

      - name: Retry Zhipu cleanup on provisional queue
        if: always()
        timeout-minutes: 10
        env:
          ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
          ZHIPU_MODEL: glm-4.5-flash
          ZHIPU_CHAT_TIMEOUT_MS: 90000
          ZHIPU_CHAT_RETRIES: 1
          ZHIPU_RATE_LIMIT_DELAY_MS: 120000
          DISCOVERY_MAX_FALLBACK_CANDIDATES: 12
          DISCOVERY_MAX_BATCHES: 4
          DISCOVERY_REFINE_BATCH_SIZE: 1
          DISCOVERY_ABORT_REFINEMENT_ON_RATE_LIMIT: true
        run: npm run papers:unofficial:refine

      - name: Upload discovery trace for debugging
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: discovery-trace
          retention-days: 30
          path: |
            data/unofficial/discovery-trace.json
            public/data/unofficial/discovery-trace.json
          if-no-files-found: warn

      - name: Commit updates
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/unofficial public/data/unofficial data/catalog public/data/catalog data/semantic public/data/semantic data/papers.catalog.json data/papers.catalog.official.json public/data/papers.catalog.json
          if git diff --cached --quiet; then
            echo "No unofficial paper changes."
            exit 0
          fi
          git commit -m "chore: refresh unofficial paper discoveries"
          git push
